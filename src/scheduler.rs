// Phase 3: Maintenance window / quiet hours enforcement
// Prevents new file copies from starting outside the configured schedule window.
// In-progress copies finish; FUSE passthrough is always unaffected.
